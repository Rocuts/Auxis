.DS_Store
.env
Documents/
__pycache__/
.venv/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.vercel
.env*
# ...but .env.example is a deliverable: anti-goal #10 requires it to
# document every required variable. It holds placeholders only.
!.env.example
# Runtime lock held while a pipeline run owns the database.
.fanout-active
